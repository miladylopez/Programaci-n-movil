import 'package:flutter/material.dart';

class CreateContact extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Crear Contacto"),
      ),
      body: Center(
        child: Text("Pantalla para crear un contacto"),
      ),
    );
  }
}
